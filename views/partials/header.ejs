<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
  <title>요아정 길드 | <%= typeof pageTitle !== "undefined" ? pageTitle : "홈" %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="app">
    <!-- 상단 바 -->
    <header class="topbar">
      <div class="topbar-inner">
        <a href="/" class="brand">
          <span class="brand-emblem">🍫</span>
          <span class="brand-text">요아정</span>
        </a>
        <% if (currentUser) { %>
          <div class="topbar-user">
            <span class="topbar-user-name"><%= currentUser.nickname %>님</span>
            <form action="/logout" method="POST" class="inline-form">
              <button type="submit" class="btn-ghost-sm">로그아웃</button>
            </form>
          </div>
        <% } %>
      </div>
    </header>
